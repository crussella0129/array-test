# s14 Research Report — brief

The freeze refused the planned design (appending a byte to test_def canonical bytes =
relayout of a frozen surface, D21). The refusal surfaced the better mechanism already
present: declared env is hashed into test_def — the per-test extension channel.
Engine-recognized flags namespaced ARRAY_TEST_*. See D25 for the full record.
